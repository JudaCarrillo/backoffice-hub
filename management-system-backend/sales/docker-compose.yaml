version: "3.8"
services:
  # Django app
  main:
    container_name: backoffice_sales_management
    labels:
      svc: backoffice_sales_management
    restart: always
    build:
      dockerfile: ./Dockerfile
    volumes:
      - .:/app
    ports:
      - 8200:8000
