version: "3.8"
services:
  # Django app
  main:
    container_name: backoffice_category
    labels:
      svc: backoffice_category
    restart: always
    build:
      dockerfile: ./Dockerfile
    volumes:
      - .:/app
    ports:
      - 8200:8000
